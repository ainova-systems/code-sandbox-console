.vscode/**
src/**
out/**
node_modules/**
**/*.ts
**/*.map
esbuild.js
scripts/**
tsconfig.json
.gitignore
.gitattributes
.github/**
.claude/**
.cursor/**
.sandbox/**
docs/**
# Intelligence sources, package store and manifest — generated tool content and
# shared rules, none of it part of the shipped extension.
intelligence/**
.intelligence/**
intelligence.yaml
intelligence.lock
# Local, gitignored root instructions: vsce ignores .gitignore whenever this file
# exists, so an untracked CLAUDE.md still ships unless it is listed here.
CLAUDE.md
AGENTS.md
CONTRIBUTING.md
SECURITY.md
# Local working notes and scratch checkouts (untracked): vsce honours .vscodeignore only —
# a git exclude does NOT keep them out — so they must be listed here or they ship inside
# the published VSIX. `npx vsce ls` before every release is what catches a new one.
RELEASE_PLAN.md
.tmp/**
*.vsix
